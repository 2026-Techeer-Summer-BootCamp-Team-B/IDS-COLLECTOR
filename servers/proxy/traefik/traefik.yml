entryPoints:
  otlp-grpc:
    address: ":4317"
  web:
    address: ":80"
  traefik:
    address: ":8080"

api:
  dashboard: true
  insecure: true

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: siem-net

log:
  level: INFO

accessLog: {}
