# SPA(React Router 없이 클라이언트 사이드 뷰 전환만 쓰지만, 새로고침 시 임의
# 경로로 직접 진입할 수 있으므로) - 정적 파일과 매칭 안 되는 요청은 전부
# index.html로 넘겨서 클라이언트 라우팅이 처리하게 한다.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
